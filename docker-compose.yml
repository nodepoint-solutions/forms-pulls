services:
  pr-viewer:
    image: ghcr.io/${GITHUB_REPOSITORY:-your-org/pr-viewer}:latest
    ports:
      - "3000:3000"
    environment:
      GITHUB_TOKEN: ${GITHUB_TOKEN}
      PORT: 3000
      CACHE_TTL_MS: ${CACHE_TTL_MS:-300000}
      NODE_ENV: production
    restart: unless-stopped
